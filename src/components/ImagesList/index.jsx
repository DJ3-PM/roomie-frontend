import React from 'react';
import { Div } from './styles';

const ImagesList = ({ children }) => {
  return (
    <Div>
      {
        children
      }
    </Div>
  );
};

export default ImagesList;
